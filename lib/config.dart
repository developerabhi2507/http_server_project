// Configuration and environment variables. For instance, you can load different environment variables for development and production (e.g., database credentials, API keys).

const String sslCertPath = 'path/to/certificate.crt';
const String sslKeyPath = 'path/to/key.key';
